import classes from "./styles.module.scss";

const LoginPage = () => {
  return (
    <div className={classes["login"]}>
      <div className={classes["login__content"]}>asd</div>
    </div>
  );
};

export default LoginPage;
